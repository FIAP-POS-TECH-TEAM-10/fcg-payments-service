apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: fcgames
  labels:
    app: payments-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5003
    name: http
  selector:
    app: payments-api
